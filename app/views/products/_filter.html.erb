<ul class="list-group">
  <% @categories.each do |c| %>
  <li class="list-group-item">
     <%= c.name %>
	 <input class='pull-right' value='<%= c.id %>' type="checkbox" value="">
  </li>
  <% end %>
</ul>
